<!DOCTYPE html> <html lang="de"> <head> <meta charset="UTF-8"> <title>GitHub RAW Link Generator</title> <style> body { font-family: sans-serif; max-width: 600px; margin: 40px auto; } input, textarea { width: 100%; padding: 8px; margin-top: 8px; } code { background: #eee; padding: 4px 6px; display: block; margin-top: 8px; } </style> </head> <body> <h2>GitHub RAW Link Generator</h2> <p>Dateipfad aus GitHub eingeben (z. B. <code>0000/Logos/Datei.png</code>):</p> <input id="path" placeholder="Pfad im Repo eingeben …"> <p>RAW‑Link:</p> <code id="output"></code> <script> document.getElementById("path").addEventListener("input", function () { const repo = "frf-krp/KRP"; const path = this.value.trim(); if (!path) { document.getElementById("output").textContent = ""; return; } const raw = `https://raw.githubusercontent.com/${repo}/main/${path}`; document.getElementById("output").textContent = raw; }); </script> </body> </html>
